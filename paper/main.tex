\documentclass[11pt]{article}

\usepackage[a4paper,margin=2.6cm]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{natbib}
\usepackage[hyphens]{url}
\usepackage{caption}
\usepackage{placeins}
\usepackage[hidelinks]{hyperref}

\captionsetup{font=small,labelfont=bf}
\newcolumntype{L}{>{\raggedright\arraybackslash}X}
\newcommand{\pending}[1]{\textit{[Pending: #1]}}
\newcommand{\sqm}{m\(^2\)}

\title{Isobenefit Urbanism on real terrain: an open-source QGIS plugin
for simulating and planning walkable growth}

\author{%
Gareth Simons\thanks{Bartlett School of Planning, University College
London.} \and Tommaso Gabrieli\thanks{Bartlett School of Planning,
University College London. \pending{confirm the corresponding author.}}}

\date{}

\begin{document}

\maketitle

\begin{abstract}
\noindent
Isobenefit Urbanism proposes settlements in which every home lies
within a walk of a mixed-use centre and of green space. The published
model grows such settlements on an abstract plain, where a few rules
guarantee the walks while the urban form emerges. Real places arrive
with existing towns, fragmented green space, rivers, railways,
motorways, and steep slopes, and with the transit around which policy
already organises growth. This paper adapts the model to real terrain
and implements it as an open-source QGIS plugin, so that the rules
become an instrument for steering development on an actual site. The
plugin prepares a scenario from OpenStreetMap and elevation data, or
from the user's own layers, keeps existing settlements as they stand,
and simulates growth within the land released for development. The
planner steers the outcome through the scenario settings: the walking
distances, the population target for new residents, the density tiers
and their shares, the land withheld from development, and the
existing or proposed transit toward which growth can be drawn. The result
is a set of plan options written as GIS layers, in which every new
home keeps a mixed-use centre and green space within the chosen
walks. Three hypothetical cases demonstrate the pipeline: a new
settlement at Cambourne, a green-belt release at Crews Hill in
London, and a hillside expansion at Pajarito in Medell\'in, each
sensitive to a different setting. The outputs are scenario
sketches for discussion and feedback at the early stages of planning.
All code, scenario data, and parameters are open, and every result
rebuilds from committed sources at fixed seeds.
\end{abstract}

\section{Introduction}

Isobenefit Urbanism \citep{dacci2019} proposes that the benefits of a
settlement should be distributed evenly. Its prefix \emph{iso} means
equal: wherever one lives, a local centre with shops, services, and
workplaces should be within a walk, and so should green space.
\citet{dacci2013} introduced the concept in a futures study and later
formalised it as a cellular-automaton morphogenesis, published
with a Python simulator \citep{dacci_voto2023,voto_code}. The automaton
grows a grid of land cells from seeded centralities (the published
model's term for centres) under a small set of rules that maintain
the walking limits as the settlement grows. The approach sits between master-planning and
laissez-faire growth, because a few simple rules guide development while
the detailed form emerges spontaneously \citep{dacci_banister_white2024}.

The published model runs on an abstract grid, in which every cell
starts as nature, distances are straight lines, and a single radius,
typically about a fifteen-minute walk, governs access both to
centres and to green space. This abstraction keeps the morphology of the
rules easy to study. On a real site the model meets existing towns,
fragmented green space, rivers, railways, motorways, and steep
slopes, together with the transit around which policy already
organises growth; the walkable-access premise of Isobenefit Urbanism is shared
by transit-oriented development, which holds that growth and density
belong within walking reach of transit \citep{calthorpe1993,
cervero_kockelman1997, ibraeva2020}. Carried onto such a site, the
rules can steer where development goes.

This paper operationalises Isobenefit Urbanism as a QGIS plugin, an
instrument planning workflows can incorporate for conceptualising
options and consulting residents and experts
\citep[cf.][]{waddell2002}. The plugin prepares the inputs, keeps
existing settlements as they stand, and grows new development within
the land a scenario releases. The planner steers the outcome through
the scenario settings: the two walking distances, the population
target for new residents, the density tiers and their shares, the
land withheld from development, and the existing or proposed transit
toward which growth can be drawn. The results are post-processed into
plan options, alternative centre arrangements on the same grown
fabric, and written as GIS layers.

The contributions are:

\begin{enumerate}
\item An adaptation of the published rules to real geography. The
  model keeps existing settlements and protected green space as they
  stand, simulates growth within land released for development, and
  measures access as walks that detour around barriers and terrain.
  Departures from the published model are tabulated
  (Table~\ref{tab:rules}).
\item An open, reproducible implementation: a QGIS plugin that wraps a
  simulation engine published on PyPI, with documentation and the
  case-study scenarios.
\item Enhancements beyond the published model. The single radius is
  separated into two walking distances, one to a centre and one to
  green space, each with its own rules. Transit-oriented development
  enters as an optional heuristic: transit hubs anchor centres, and a
  corridor preference, zero by default, draws growth toward existing
  or proposed transit. Centres are founded on the population that
  justifies them rather than by a seeding probability, which ties
  the number of settlements to the service viability threshold
  instead of to the window's size and the grid resolution.
  Post-processing optimises the placement of centres and
  grades the densities from centre to edge, and the plan options keep
  every new home within the centre walk.
\end{enumerate}

We demonstrate the pipeline on three case studies, each dominated by a
different constraint and steered through a different setting.
Sections~\ref{sec:background} to~\ref{sec:discussion} cover the background,
the methods, the case studies, and the discussion.

\section{Background}
\label{sec:background}

\subsection{The published model}

The Isobenefit-cities simulator \citep{dacci_voto2023} belongs to a
long line of cellular models of urban form with real-world planning
applications \citep{white_engelen1993,
batty2005, sante2010}, and differs from models fitted to observed
growth in that its rules enforce a walkability guarantee while growth
proceeds. It grows settlements on a
square grid in which every cell starts as nature. Each simulation step scans
the grid against an unchanged copy. A nature cell adjacent to built land may
become built on two conditions: a centrality must lie within the
radius T\(^*\), typically about one kilometre or a fifteen-minute
walk, and building the cell must not breach the green-space rules,
which keep nature as one connected region whose width does not fall
below T\(^*\). An eligible cell builds by a random draw at each
step, so the built edge advances irregularly. New
centralities seed by rarer draws, either beside built land that has
outgrown its centres or on open land. Each built block takes its
population density from one of three tiers, high, medium, or low,
drawn at set shares, and the run stops when the total population
reaches its cap.

\subsection{Walking distances and standards}

We replace the single radius with two walking distances, one to a
centre and one to green space. Both are set in the QGIS plugin, whose
defaults of 1{,}200~m for the centre walk and 400~m for the
green walk are approximately fifteen-minute and five-minute walks
respectively. The walkable-city literature generally treats
destinations within a ten-to-fifteen-minute walk as walkable
\citep{moreno2021, abbiasov2024}, and observed walking distances for
everyday errands are of the same order \citep{yang_diezroux2012}. For
green space, the WHO Regional Office for Europe advises everyday access
within about 300~m straight-line distance \citep{who2016}, and Natural
England's accessible natural greenspace standard uses the same figure
\citep{naturalengland2010}. Both standards also set a minimum size
for qualifying green space: the WHO advice concerns spaces of at
least half a hectare to one hectare, and the Natural England standard
applies from 2 hectares, which is the default for the
minimum park area (Section~\ref{sec:growth}). The 400~m default is slightly
longer than the advised 300~m because a walking route goes around
obstacles and covers more distance than the straight line.
Transit adds two further distances (Section~\ref{sec:transit}). The
stop catchment, 400~m by default, bounds the walk to a bus stop or a
drawn transit route, and matches the quarter-mile bus-stop catchment
conventional in transit-oriented development \citep{calthorpe1993,
ibraeva2020}. The hub catchment, 1{,}200~m by default, bounds the
walk to a station or a designated hub. Station catchments in the same
practice start at half a mile, and the default matches the centre
walk, because a hub anchors a centre.
Section~\ref{sec:cases} sweeps the centre walk across 800, 1{,}200,
and 1{,}600~m; the green walk stays at 400~m throughout.

\section{Methods}
\label{sec:methods}

The QGIS plugin comprises two tools, one for data preparation and one
for simulation. The preparation tool downloads a scenario's input
layers from OpenStreetMap. Users can instead supply equivalent
layers from their own data, or download the layers and edit them.
The simulation tool passes these inputs to the
engine, published on PyPI, which applies the growth rules and the
post-processing. The subsections below follow that sequence.

\subsection{Real inputs}
\label{sec:inputs}

Each scenario is defined by a simulation boundary in a metric coordinate
reference system and a set of vector layers. The case-study scenarios
fetch these layers from OpenStreetMap
\citep{osm} with the preparation tool. The layers cover existing
built fabric, protected green space, existing mixed-use centres,
unbuildable land (the areas treated as unavailable for development),
streets, public-transport stops, rail and tram stations,
railways, and industrial land, which counts as unbuildable for new
homes. Terrain slope is mapped from the Copernicus GLO-30 elevation model
\citep{copernicus_glo30} as bands steeper than 15, 20, 25, and 30
degrees. The bands are a separate layer that users can edit from
local knowledge.

\subsection{Grid preparation}
\label{sec:gridprep}

The simulation tool rasterises the input layers to a working grid at a
user-selected cell size, such as 50~m, though a finer grid may be
preferable where the extent and the available computation allow. Each
cell takes its class from the layer that intersects it:
existing built fabric, green space, or unbuildable land. A cell that
intersects none of the layers is marked open land. The tool then adds
to the unbuildable class in two ways. It carves corridors along
motorways, trunk roads, railways, and rivers, and it marks the slope
bands that lie above the scenario's slope limit. The remaining street
geometry is kept for display only.

The model treats new development and the existing fabric differently.
The existing fabric stays as mapped, and the model does not redevelop it,
for two reasons. First, the emphasis is on new development, so the
population target and the walking distances concern new homes, and
the constraints in the sections that follow are derived from and
applied to the new growth. Second, the input data rarely describe
existing densities and services in sufficient detail to model them.

The simulation tool then screens the open land against the service
viability threshold, the population a minimal local centre needs;
the default of 2{,}000 people sits at the small end of published
facility catchments \citep{barton2021}. A region too small to hold
the threshold population is kept when it lies within the centre
walk of an existing centre, where new growth is infill served by
that centre. The rest, typically small islands and slivers of open
land enclosed by existing development or unbuildable corridors, is
excluded from new development before the run.

The growth rules and all reported metrics measure
distance with the bounded grid walk: the shortest path over
traversable cells, which detours around unbuildable land and is cut
off at the maximum walking distance. The path is computed by
Dijkstra search.

\subsection{The growth rules}
\label{sec:growth}

A run proceeds in iterations. Each iteration applies the growth
rules to every land cell, visiting the cells in a shuffled order to
avoid the directional bias of a fixed scan. A cell is a candidate
for building when it is empty, buildable, touches built land, and
lies within the centre walk of a centre.
New growth attaches only to built land that carries a centre, so
existing settlements without one, such as outlying farmsteads and
hamlets, receive no new growth. A run starts with the existing mixed-use
centres and with a centre seeded at each transit hub, a rail or tram
station or a point the planner designates, and development gathers
around the hubs from the first iteration.

Building a candidate must leave the green network intact. A park is
a contiguous green area of at least the minimum park area, 2
hectares by default, which follows Natural England's accessible
natural greenspace standard \citep{naturalengland2010}. New
development may not shrink a park below the minimum, whether by
splitting it or by building along its edge, and a green corridor
between two settlements may not narrow below a minimum span. Green
that one settlement encloses on its own, in the crook of a C or a U,
is a bay rather than a corridor, and the settlement may build into
it. Every built cell, the existing fabric included, must keep a park
within the green walk.

A candidate that passes every check builds by a random draw. The draw
staggers the built edge so that ensemble members differ; it paces
growth without shaping the plan, and is not a setting. Each built
cell draws one of the three density tiers
according to the shares, and a run stops when it reaches the
population target.

New centres are earned rather than drawn. Each new home is charged to
the centre it is nearest, measured by the grid walk, so catchments
partition and a resident counts once. When a centre has gathered the
service viability threshold, it releases one new centre, which is
placed at the next cell the scan reaches that lies beyond the centre
walk of every existing centre. Growth therefore creates centres at
the rate the population justifies, and each centre carries a viable
settlement's population before the next is founded. A centre also
releases its successor when it can no longer grow, which is what
happens where terrain confines a catchment to less land than the
threshold needs; without that provision growth would halt with the
target unmet. One setting governs where a released centre may go:
either it must attach to existing development, or it may start a
satellite away from it.

A scenario may also ask growth to favour land that transit serves,
in which case the build draw above, and the placement of a released
centre, are weighted in its favour (Section~\ref{sec:transit}).
Unless the planner asks for this, both stand as described.

\subsection{Ensembles and selection}
\label{sec:ensemble}

The growth rules are stochastic, so a single run is one pattern
among the many the same inputs allow. The tool therefore runs an
ensemble, fifty runs in the scenarios reported here. Each run
starts from the same inputs with its own random seed and grows a
complete settlement pattern that satisfies the constraints, so
runs build in different places. The likelihood layers
summarise this variation by recording, for each cell, the fraction
of runs in which it ended built or green, which indicates how
likely an area is to develop under the constraints.

Because the runs vary cell by cell, averaging them blends the
constraints away; the blended result no longer satisfies the walking
limits, so the layers are informational only. We instead select the
best single run, the one whose candidate plan from the
post-processing of Section~\ref{sec:fouroutputs} scores lowest.
When a run houses the whole target within the walks, its score is
the mean walk of its new homes to a centre and to green
space. Two penalty terms apply otherwise: a home beyond
either walk enters the average at twice that walk's limit, and
each unhoused person of the target enters as a home beyond both
walks. A run therefore cannot improve its score by housing fewer
people.

The mean walks reported in this paper include no penalty terms;
each averages over the new homes that reach a centre or green
space within the walk.

\subsection{Post-processing and the four outputs}
\label{sec:fouroutputs}

Post-processing turns the best single run into the four outputs.
The first, the raw grown state, is the run exactly as it grew,
coloured by the density tiers it drew. The other three are plan
options: the same fabric and drawn densities under three centre
arrangements. No post-processing edit may leave a new home beyond
the centre walk of a centre.

Post-processing begins with viability. Each centre is
re-positioned central to the new homes it serves and must gather
at least the threshold population within the centre walk. A centre
that falls short is removed, and growth left without a centre
within the walk is pruned back to nature. Existing centres, the
hub centres, and the existing fabric are untouched, and pruning is
the only step that removes growth.

The centres are then arranged, in one of three ways.
\emph{As grown} keeps the run's centres where they
seeded, adding one only where pruning removed a cluster's serving
centre. \emph{Optimised placement} keeps each centre at the
position central to the new homes it serves, which shortens their
mean walk, and adds a centre wherever new development lacks
provision within the walk. \emph{Fewest centres} starts from the optimised
placement and removes the most redundant centre, one at a time,
for as long as every home keeps a centre within the walk: the
fewest centres the walk constraint permits. Infill too small to
meet the threshold is served by an existing centre within the walk
and receives no centre of its own. Optimised placement is the
selected plan; run selection scores each run's optimised-placement
candidate, and the case studies report that option.

The densities are arranged last, once the centres are final.
Within each settlement the drawn values are redistributed to grade
from a dense core around the centres to a lighter edge; the shares
and the population are unchanged.

A centre is a mixed-use district whose cells carry homes at the
arranged densities alongside floor area for services. Each centre
is sized by the new residents nearest to it, at 20~m\(^2\) of
centre area per resident by default, and each resident counts
toward one centre. The
default covers the shops, services, and workplaces of
neighbourhood-centre standards \citep{barton2021}. Because amenity
provision scales sublinearly with settlement size
\citep{bettencourt2013}, a single ratio suits the small-to-mid
settlement growth the tool addresses.

An ensemble run loads its results into the QGIS project as one
layer group: the existing fabric for comparison,
the raw grown state, and the three plan options as categorical
rasters, each coloured by density tier with the centres in a
distinct hue and the centre count in the layer name. A further
layer maps the growth that pruning removed, and the two likelihood
layers complete the group. Alongside the layers the tool writes a
plain-text run report listing the parameters, the run summary,
the four outputs' metrics side by side, and the achieved shares
and centre audit, so every post-processing edit stays visible. A
single run, outside the ensemble, is written instead as a
temporal animation of the growth, one frame per step.

\subsection{Transit-oriented growth}
\label{sec:transit}

Where a planner wants growth to follow transit, two settings
express that intent, and a scenario that leaves them alone runs the
growth rules as the preceding sections describe.

The first is the hubs layer. Each transit hub seeds a centre at the
start of a run, so development gathers around it, and
post-processing pins the centre, which is neither pruned nor
repositioned and is sized like any other. Stations enter the layer
with the scenario's other inputs; a point added by hand designates
a centre where the planner intends one, such as a transit-oriented
development around a planned station.

The second is the corridor preference, which sets how strongly
growth favours the land transit serves. That land is the transit
catchment: the area within the stop catchment of a bus stop or a
drawn route, or within the wider hub catchment of a hub, measured
by the grid walk. Inside it, development proceeds at the ordinary
likelihood, and a released centre may be placed there; outside it,
both are reduced in proportion to the preference. At a
preference of 0.5 development outside the catchments is half as
likely as inside, and at 0.95, the setting used in the
demonstration below, a twentieth as likely, which is enough to
string a new settlement along a route. The default is 0, at which
the catchments carry no weight and growth follows the rules of
Section~\ref{sec:growth} alone; the case studies use this default
except where stated.

Whatever the settings, the run report gives the share of new homes
within the transit catchments and the mean walk to the nearest
transit stop or hub, so a plan grown without any transit intent can
still be read against the transit that exists. These metrics do not
enter run selection.

\subsection{Rule adaptations}
\label{sec:rules}

The reference for every comparison in this section is the published
model as formalised by \citet{dacci2019} and coded in the
Isobenefit-cities simulator \citep{dacci_voto2023, voto_code}: its
rules as implemented there, on the uniform abstract plain, at its
published defaults. The comparison is with that reference
implementation rather than with Isobenefit Urbanism as a concept,
which admits realisations other than the automaton.

Against that reference, Table~\ref{tab:rules} records the treatment
of each rule under three statuses: unchanged (the mechanism is the
same, with its parameters exposed as settings), modified (the
behaviour differs), or extension (no counterpart in the reference).
Most of the changes carry the model from the abstract plain to the
real inputs of Section~\ref{sec:inputs}.

\begin{table}[!tp]
\centering
\caption{The published rules \citep{dacci2019, dacci_voto2023, voto_code}
and their treatment in this implementation.}
\label{tab:rules}
\footnotesize
\begin{tabularx}{\textwidth}{L L l}
\toprule
Published rule & This implementation & Status \\
\midrule
The grid starts as uniform nature &
Real inputs: existing fabric held fixed, protected green space,
unbuildable land, existing centres, and transit &
Extension \\
\addlinespace[2pt]
Build on a nature cell adjacent to built land &
Same, against fabric that carries a centre; outlying farmsteads and
hamlets anchor nothing &
Modified \\
\addlinespace[2pt]
A centrality lies within T\(^*\), measured straight-line &
A centre lies within the centre walk, a bounded walk over the grid
that cannot cross unbuildable land &
Modified \\
\addlinespace[2pt]
Build by a random draw at a set probability &
Same draw, at a fixed rate: it paces the edge and varies the ensemble,
and does not shape the plan &
Unchanged \\
\addlinespace[2pt]
Nature stays one connected region at least T\(^*\) wide &
Local rules: a corridor between two settlements keeps a minimum span,
and a park keeps a minimum area; green a settlement encloses on its
own is a bay it may build into &
Modified \\
\addlinespace[2pt]
Every built cell keeps nature within T\(^*\) &
Same rule, at its own distance (the green walk), and it binds the
newly built cell too &
Unchanged \\
\addlinespace[2pt]
New centralities seed near under-served built land by a draw &
Earned, not drawn: a centre founds the next once the new population
nearest it reaches the viability threshold, or once it cannot grow &
Modified \\
\addlinespace[2pt]
Isolated centralities seed on open land by a draw scaled by area &
No draw: a released centre may start away from existing development
where the scenario allows detached settlements &
Modified \\
\addlinespace[2pt]
One radius T\(^*\) governs centres and nature alike &
Two distances in metres: the centre walk (1{,}200~m) and the green
walk (400~m) &
Modified \\
\addlinespace[2pt]
Each block draws a density from three tiers at set shares &
Same draw; post-processing grades the drawn values from a dense core
to a lighter edge &
Modified \\
\addlinespace[2pt]
The run stops at a population cap &
Same stop; the target counts new residents only &
Modified \\
\addlinespace[2pt]
Cells are scanned in a fixed raster order &
Shuffled each iteration, removing the scan-direction bias &
Modified \\
\addlinespace[2pt]
All nature is available to build &
Land too narrow, or in a region too small to hold a viable
settlement, is set aside as protected green before the run &
Extension \\
\addlinespace[2pt]
Growth is spatially uniform &
Transit hubs pin a centre, and a corridor preference concentrates
growth within the transit catchments; both are optional &
Extension \\
\addlinespace[2pt]
One run, one output &
An ensemble yields likelihood layers, and the best-scoring run is
selected &
Extension \\
\addlinespace[2pt]
Centralities stay where they seeded &
Three centre arrangements in post-processing, each keeping every new
home within the centre walk of a centre &
Extension \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{Software and reproducibility}
\label{sec:software}

The implementation comprises a simulation engine and a QGIS plugin.
The engine is a Rust crate, published on PyPI as
\texttt{isobenefit}, whose small Python interface, built with PyO3,
accepts and returns arrays; the engine does not import QGIS. The
QGIS plugin \citep{qgis} supplies the data preparation, the run
dialog, and the post-processing and reporting, and depends only on
what QGIS already bundles plus the engine. The QGIS plugin repository does not permit
shipping binaries, so the compiled engine installs from PyPI and the
plugin offers to install it on first run.

Results are deterministic and independent of thread count, because
every stochastic draw derives from its own fixed seed and parallel
totals are integer sums whose order does not matter. The test suite
cross-checks the Rust engine against a reference Python
implementation of the bounded grid walk and of the labelling of
contiguous regions. A single verification script runs the same
checks as the continuous-integration pipeline (formatting, linting,
Rust and Python tests, and the website build); releases are tagged,
version-locked between engine and plugin, and published
automatically. All scenario inputs are committed, so every result
in this paper rebuilds offline from source at fixed seeds.

\section{Case studies}
\label{sec:cases}

We ran the pipeline on three demonstration cases chosen for contrast
in the land they offer: a
free-standing new settlement (Cambourne, UK), a green-belt release at a
metropolitan edge (Crews Hill, London), and a topography-bound hillside
expansion (Pajarito, Medell\'in). All three hold their target, so what
the cases separate is the setting each is most sensitive to, and each
case's figure varies that setting: the centre
walk at Cambourne, the shares at Crews Hill, and attachment and the
viability threshold at Pajarito.
All three run at the default corridor preference of 0; Cambourne,
the reference case, then repeats with the transit settings raised,
which is the only run in the paper that uses them.
Table~\ref{tab:scenarios}
gives the settings, and a closing centre-walk sweep covers all
three cases.

The cases demonstrate how a planner steers the tool; their windows,
targets, and density presets are hypothetical. An
application in practice would set these in consultation with local
residents, planners, and experts.

All metrics below come from full-resolution runs of the committed
scenarios (fifty-run ensembles at the grids of Table~\ref{tab:scenarios},
seed 42).

\begin{table}[t]
\centering
\caption{Case-study settings. Tiers are people per km\(^2\) (high / medium /
low) with shares in the same order; all cases use a 1{,}200~m centre walk,
400~m green walk, a 2{,}000-person service viability threshold, 400~m minimum green
span, a 2-hectare minimum park area, and fifty ensemble runs.}
\label{tab:scenarios}
\small
\begin{tabularx}{\textwidth}{l L L L}
\toprule
 & Cambourne & Crews Hill & Pajarito \\
\midrule
Theme & New settlement & Green-belt release & Hillside expansion \\
CRS / grid & EPSG:27700, 50~m & EPSG:27700, 25~m & EPSG:32618, 25~m \\
Target (new residents) & 15{,}000 & 13{,}000 & 40{,}000 \\
Tiers & 6{,}000 / 3{,}000 / 1{,}500 & 26{,}000 / 15{,}500 / 9{,}500 &
35{,}000 / 18{,}000 / 8{,}000 \\
Shares & 0.2 / 0.3 / 0.5 & 0.2 / 0.5 / 0.3 & 0.3 / 0.4 / 0.3 \\
Dispersed development & Detached allowed & Attached only & Detached allowed \\
Slope limit & None & None & Over 20\(^\circ\) unbuildable \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{Cambourne: a new settlement and the four outputs}

Cambourne is a fast-growing Cambridgeshire new settlement. The
15{,}000 target is hypothetical and reflects no assessment of
local housing need. This reference case presents the four outputs
(Table~\ref{tab:fouroutputs}), varies the centre walk, and ends with
the transit demonstration, a proposed
corridor and hub. Figure~\ref{fig:cambourne}
shows the selected plan under the
baseline and 1{,}600~m centre walks. At the baseline 1{,}200~m
walk, the plan accommodates 15{,}411 new residents against a
15{,}000 target, and they average a 276~m walk to a centre and
124~m to green space. Table~\ref{tab:sweep} sweeps the walk.

Table~\ref{tab:fouroutputs} reproduces the Cambourne scenario's run
report, which records the four outputs. Because growth founds a
centre only where the population justifies one, nothing is pruned
here: the raw state and the plan options house the same 15{,}411
residents on the same 3{,}904 cells, and the options differ in
their centres alone. The counts include the 20 existing centre
areas the window already has, so the run's 22 areas are the
existing ones plus the two it earned, and the plan's 30 are those
plus the centres post-processing adds where new development has
none within the walk. What separates the options is the walk: 606~m
as the run left its centres, 276~m once each is moved central to
the homes it serves, and 295~m under fewest centres, which returns
19~m for three centres fewer. A centre is founded at the edge of
the development that earned it, which is why the raw figure is the
outlier.

A final run turns the transit settings on, which the preceding runs
left at their defaults. We draw a proposed bus
corridor from Cambourne's eastern edge across the plain to the
window's north-eastern village and add a proposed bus rapid transit
stop as a transit hub at its southern end beside the A428 junction.
The corridor preference is 0.95 and every other setting stays at the
baseline. Growth and its satellite settlements string along the route
instead of spreading across the window (Figure~\ref{fig:corridor}),
and the share of new homes within the transit catchments rises from
9\% to 88\%. The hub lies just beyond the centre walk of the
existing centres, so its catchment holds land they do not serve, and a new
settlement gathers around its pinned centre at the junction. The
population accommodated moves little, 14{,}592 against 15{,}411,
because the catchments hold enough developable land for a
settlement of this size; what changes is where it sits. The mean
centre walk lengthens from 276 to 298~m, since growth strung along
a route is served by centres spaced along it rather than gathered
around a core, while the walk to green space falls from 124 to
119~m. Every home keeps both walks. Where the catchments hold less
land the preference draws growth into a smaller area, and the run
report gives the share of homes within them either way.

\begin{table}[t]
\centering
\caption{Run report for the Cambourne scenario (15{,}000-person
target): the raw grown state and the three plan options. Walks are
measured over new homes.}
\label{tab:fouroutputs}
\small
\begin{tabular}{l r r r r}
\toprule
Metric & Raw & As grown & Optimised placement & Fewest centres \\
\midrule
Population accommodated & 15{,}411 & 15{,}440 & 15{,}411 & 15{,}411 \\
Share of target & 103\% & 103\% & 103\% & 103\% \\
Built cells (incl.\ existing) & 3{,}904 & 3{,}904 & 3{,}904 & 3{,}904 \\
Mixed-use centre areas & 22 & 30 & 30 & 27 \\
Mean walk to a centre (m) & 606 & 377 & 276 & 295 \\
Mean walk to green space (m) & 124 & 124 & 124 & 124 \\
Centre \sqm{} per person & 0 & 13 & 15 & 13 \\
Green \sqm{} per person & 3{,}570 & 3{,}564 & 3{,}570 & 3{,}570 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[!tp]
\centering
\includegraphics[width=0.48\textwidth]{figures/cambourne_baseline.png}\hfill
\includegraphics[width=0.48\textwidth]{figures/cambourne_walk1600.png}\\[6pt]
\includegraphics[width=\textwidth]{figures/legend.png}
\caption{The selected plan for the Cambourne window at the
baseline 1{,}200~m centre walk (left) and at 1{,}600~m (right).
Full scenario resolution, seed 42.}
\label{fig:cambourne}
\end{figure}

\begin{figure}[!tp]
\centering
\includegraphics[width=0.48\textwidth]{figures/cambourne_transit_off.png}\hfill
\includegraphics[width=0.48\textwidth]{figures/cambourne_transit_corridor.png}\\[6pt]
\includegraphics[width=\textwidth]{figures/legend.png}
\caption{The selected plan for the Cambourne window at the baseline
corridor preference of 0 (left) and at 0.95 (right). Both panels
overlay the drawn bus route (teal line), the existing stops (teal
dots), and the proposed hub (ringed teal marker). Full scenario
resolution, seed 42.}
\label{fig:corridor}
\end{figure}

\subsection{Crews Hill: a green-belt release}

The Crews Hill area of Enfield, inside the M25 at London's northern edge,
is one of the largest green-belt releases proposed in an emerging London
local plan, which allocates about 5{,}500 homes around an existing rail
station. We built the scenario to sketch the walkable settlement the
release could hold and the green network that survives. Growth must
stay attached, so the extension is contiguous. The density tiers use
indicative UK design-code values (40 / 65 / 110 dwellings per hectare at a
household size of 2.36). The scenario takes from the emerging plan only
the site boundary and the allocation's scale. Whether the release
should proceed is a question for the plan's examination and its
public consultation.

Under the baseline settings the plan reaches 13{,}213 of the 13{,}000
target, and new homes
average a 269~m walk to a centre and 61~m to green space.
Figure~\ref{fig:crewshill} varies the shares on the fixed boundary.
Because the release can hold the target at any of the three mixes,
the shares set the land it takes rather than the population: 0.5 /
0.3 / 0.2 (high / medium / low) houses 13{,}318 on 4{,}319 cells,
and 0.1 / 0.3 / 0.6 houses 13{,}088 on 4{,}851, a tenth more land
for the same people. The
station's hub centre is pinned against the rail line, and the
new development forms around the station.

\begin{figure}[!tp]
\centering
\includegraphics[width=0.48\textwidth]{figures/london_crews_hill_lower.png}\hfill
\includegraphics[width=0.48\textwidth]{figures/london_crews_hill_denser.png}\\[6pt]
\includegraphics[width=\textwidth]{figures/legend.png}
\caption{The selected plan for the Crews Hill release, with
attached growth, at shares of 0.1 / 0.3 / 0.6 (high / medium / low)
(left) and 0.5 / 0.3 / 0.2 (right). The two settings
house the same population, 13{,}088 and 13{,}318, on 4{,}851 and
4{,}319 cells: the denser mix returns a tenth of the land to green
space. The rail station (ringed marker) anchors a
pinned centre in both settings, and the release grows around it. Full scenario resolution, seed 42.}
\label{fig:crewshill}
\end{figure}

\subsection{Pajarito, Medell\'in: growth shaped by terrain}

Pajarito and Ciudadela Nuevo Occidente on Medell\'in's northwestern slopes
are a planned expansion of high-rise social housing served by the
Metrocable. Slopes over 20 degrees are unbuildable and cover about 30\% of
the study window, so the green network and the short walking distances
must fit around the terrain. The high density tier (35{,}000 people per
km\(^2\)) reflects the area's housing towers. The scenario is a
hypothetical demonstration on the district's terrain and carries no
recommendation for its future development.

At the baseline walk the plan accommodates 39{,}032 against the
40{,}000 target with a 116~m mean
centre walk for new homes; the steep slopes fragment the buildable
land into detached benches of gentler ground.
Figure~\ref{fig:medellin} varies the two settings that bind here.
Requiring growth to stay attached to existing development collapses
the plan to 5{,}201, because a centre may then only be founded
against fabric that already exists and the benches are unreachable
across the steep ground between them. Raising the viability
threshold from 2{,}000 to 4{,}000 people houses 38{,}630 in fewer
and larger settlements, at a 189~m mean centre walk.

\begin{figure}[!tp]
\centering
\includegraphics[width=0.32\textwidth]{figures/medellin_pajarito_baseline.png}\hfill
\includegraphics[width=0.32\textwidth]{figures/medellin_pajarito_compact.png}\hfill
\includegraphics[width=0.32\textwidth]{figures/medellin_pajarito_quota4000.png}\\[6pt]
\includegraphics[width=\textwidth]{figures/legend.png}
\caption{The selected plan for the Pajarito window under three
settings: the baseline (left), attached growth only
(centre), and a 4{,}000-person viability threshold (right). Slopes over
20\(^\circ\) are unbuildable and fragment the buildable land into
benches. Full scenario resolution,
seed 42.}
\label{fig:medellin}
\end{figure}

\subsection{Sensitivity to the centre walk}

The centre walk is swept across 800, 1{,}200, and 1{,}600~m for the
three cases. The population accommodated moves little across the
sweep: Cambourne houses 13{,}416 to 15{,}411, Crews Hill 13{,}005
to 13{,}318, and Pajarito 38{,}655 to 39{,}032. What the walk
changes is how many centres serve those residents and how far they
walk to one.

A centre is founded once the population nearest it reaches the
viability threshold, so a wider walk lets a centre gather that
population from more land, and fewer centres are founded. Crews
Hill shows this plainly, at 84~m under an 800~m walk against 269~m
at the baseline. The mean walk does not rise with the setting in
every case, because the walk also bounds how far a home may sit
from its centre, and the two effects meet differently on each
window's land. Each figure comes from one selected run, and the
setting changes which run is selected.

\begin{table}[t]
\centering
\caption{The mean centre walk of new homes under three centre-walk
settings.}
\label{tab:sweep}
\small
\begin{tabular}{l r r r}
\toprule
Scenario & 800~m & 1{,}200~m (baseline) & 1{,}600~m \\
\midrule
Cambourne & 317~m & 276~m & 380~m \\
Crews Hill & 84~m & 269~m & 262~m \\
Pajarito & 113~m & 116~m & 116~m \\
\bottomrule
\end{tabular}
\end{table}

\section{Discussion}
\label{sec:discussion}

\subsection{Effects of the adaptations}

Real terrain changes the meaning of the walkability guarantee. On
the abstract plain the guarantee follows from the rules, but on
real inputs it becomes a property of the resulting plan and can
be checked against the map. Results here do not compare directly
with published abstract-plain morphologies, because the walk metric,
the green-space rules, and the seeding rules differ, as
Table~\ref{tab:rules} records.

The shared bounded grid walk for growth and scoring is the largest
departure. A street-network metric is common in walkability
analysis, and earlier releases used one over the existing fabric
with grid walks over new growth, which has no streets until a later
design stage. We removed the hybrid because a distance measured
partly over streets and partly over the grid has no consistent
meaning. The bounded grid walk with carved barriers treats new and
existing fabric on the same terms and does not cross motorways,
railways, or rivers.

\subsection{What post-processing contributes}

The published model leaves a centrality where it seeded and leaves each
block at the density it drew. Both are reasonable on an abstract plain,
where a centrality seeds in open country and the settlement grows around
it symmetrically. On real terrain neither holds, and the three
post-processing steps of Section~\ref{sec:fouroutputs} address what
breaks.

Placement is the clearest gain. A centre is founded at the edge of the
development that earned it, because that is where growth had reached,
and the settlement then grows past it. At the Cambourne baseline the new
homes average a 606~m walk to the centres as the run left them, 377~m
once the centres pruning left missing are supplied, and 276~m once each
centre is moved to the point central to the homes it serves. That last
step changes no fabric, no population and no centre count; it moves 30
centres. Fewest centres gives 295~m for three centres fewer, the
arrangement to reach for when provision is expensive.

Viability is the second. The growth rules found a centre only where the
population justifies one, so the pass has nothing to remove at the
Cambourne baseline: the raw state and the plan options house the same
15{,}411 residents. The pass now works as an audit. It checks every
surviving centre against the threshold in the run report, and it
enforces through each edit, rather than checking at the end, that no
plan option leaves a home beyond the centre walk of a centre.

Two details of the accounting matter for reading the centre counts. A
centre passes the viability test on the new homes within its centre
walk, and those catchments overlap, at Cambourne by a factor of 2.3, so
neighbouring settlements pool the demand that keeps their centres
viable. The counts also include the centres the window already has: 20
of the Cambourne plan's 30 areas are existing centres, which serve the
existing town and count as no provision for new development, so new
growth earns its own.

Density arrangement is the third, and it is a presentational claim
rather than a modelling one. The growth rules draw a tier for each cell
from the shares, which fixes the mix and therefore the population, but
they draw it when the cell is built, before the settlement that will
surround it exists. Arranging those drawn values so the highest tiers
sit at the core and the lowest at the edge produces the density gradient
that a plan drawing is expected to show, and because the values are only
rearranged, the shares and the population are unchanged.

The boundary matters for how the outputs should be read. Growth produces
the raw state; post-processing improves how that state is served and
presented, and moves no one into or out of the window. The raw layer
ships with every run so the two can be compared directly.

\section{Conclusion}
\label{sec:conclusion}

This implementation builds on the published model and turns Isobenefit
Urbanism into an instrument a planner can run on a real place and
steer through its settings. It
keeps the published growth rules recognisable and adapts them to real
geography through the departures that Table~\ref{tab:rules} records. The
plan-derivation pipeline scores every run on walkable access, keeps
the best single run, and derives the four outputs, with the walk
constraint enforced in every option. All three case studies hold their
population target, and what separates them is the setting each
answers to: the centre walk governs how many centres serve
Cambourne, the density shares govern how much of the Crews Hill
release is built on, and at Pajarito the terrain decides whether
growth may detach at all. The Cambourne corridor demonstration shows the transit
levers steering growth to a proposed route while retaining more of
the target. The
software, data, and parameters are open,
and the results rebuild from committed sources at fixed seeds.

The case studies are hypothetical demonstrations, and a live
application would begin in consultation with local residents and
planners.

\subsection*{Acknowledgements}

This work is part of the Future Urban Growth project at the Bartlett School
of Planning, University College London. The authors thank the wider project
team: Luca D'Acci, Heeseo Rain Kwon, Stephen Marshall, and Valentina Marin
Maureira. The collection's guest editors, T.~Gabrieli (a co-author) and
L.~D'Acci (a member of the wider project team), are both connected to this
work; the submission is handled independently under the journal's
guest-editor policy.

\subsection*{Data availability}

All scenario inputs (boundaries, parameter presets, OpenStreetMap layers,
and slope bands) are committed in the project repository at
\url{https://github.com/UCL/BSP-isobenefit-qgis-plugin} under the
\texttt{scenarios/} directory. OpenStreetMap data \copyright{}
OpenStreetMap contributors, available under the Open Database License.
Slope layers derive from the Copernicus GLO-30 digital elevation model.

\subsection*{Code availability}

The simulation engine is published on PyPI as \texttt{isobenefit} and the
QGIS plugin through the official QGIS plugin repository; both are developed
in the repository above under the AGPL-3.0-or-later licence.
\pending{archive a tagged release on Zenodo and cite the DOI here.}

\subsection*{Author contributions}

\pending{to be completed with the project team.}

\subsection*{Ethics declarations}

This article does not contain any studies with human participants or
animals performed by any of the authors, and informed consent is not
applicable.

\subsection*{Competing interests}

The authors declare no competing interests. L.~D'Acci, originator of
Isobenefit Urbanism and a guest editor of this collection, is a member of
the wider project team but not an author of this article; T.~Gabrieli is
also a guest editor of the collection and will take no part in its
editorial handling. \pending{confirm wording with the journal.}

\FloatBarrier
\bibliographystyle{apalike}
\bibliography{refs}

\end{document}
